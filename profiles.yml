dbt_demo:
  target: dev
  outputs:
    dev:
      type: bigquery
      method: oauth
      project: "{{ env_var('GCP_PROJECT_ID') }}"
      dataset: "{{ env_var('DBT_DATASET_NAME') }}"
      job_execution_timeout_seconds: 300
      threads: 4
